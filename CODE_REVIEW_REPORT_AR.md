# تقرير مراجعة الكود — مشروع "حيلهم بينهم"

هذا تقرير تقني شامل بناءً على العمل اللي اتعمل على المشروع طول الوقت اللي فات: تنظيم الكود ككل، هل نستخدم Redux Toolkit، وأهم الأخطاء والثغرات والحاجات الغير منطقية الموجودة حاليًا.

---

## 1. نظرة عامة على حجم المشروع

| الملف | عدد الأسطر |
|---|---|
| `app/battle/page.jsx` | ~1958 سطر |
| `app/admin/page.jsx` | ~1933 سطر |
| `components/battle/RefereeGameScreen.jsx` | ~1040 سطر |
| `components/GameSetupSection.jsx` | ~788 سطر |

المشروع مبني بـ Next.js (App Router) + Supabase (قاعدة بيانات + Auth + Realtime + Storage)، وكل منطق اللعبة تقريبًا شغال عن طريق دوال Postgres (RPCs) بصلاحية `SECURITY DEFINER` — وده أسلوب سليم أمنيًا (الكلاينت مايقدرش يلمس الجداول الحساسة مباشرة).

**المشكلة الأساسية اللي هتلاحظها في التقرير ده كله:** مفيش أي ملفات migrations متتبّعة في الريبو (لا فولدر `supabase/migrations`، ولا حتى ملف `schema.sql`)، ومفيش أي اختبارات (tests) خالص — لا `jest`, لا `vitest`, لا `playwright`. كل تعديل SQL اتعمل طول الوقت مباشرة في Supabase SQL Editor بدون ما ينحفظ كملف في الريبو. النقطتين دول هما أخطر حاجة في المشروع حاليًا، وهفصّلهم تحت.

---

## 2. أهم المشاكل والفجوات المكتشفة

### 2.1 مفيش نسخة من قاعدة البيانات متتبّعة في الكود (خطر عالي)

كل تعديل SQL (إنشاء جداول، تعديل RPCs، إضافة أعمدة) اتنفذ يدويًا في Supabase SQL Editor. النتيجة:
- لو حصل أي مشكلة في قاعدة البيانات الحية، مفيش طريقة تعرف بيها "الشكل الصح" للسكيمة غير إنك تدخل تفحص كل دالة وكل جدول واحد واحد من جوه Supabase نفسه.
- لو حد عمل clone للريبو من الصفر، مش هيعرف يشغّل المشروع محليًا لأن مفيش ملف SQL يبني بيه قاعدة بيانات جديدة من الصفر.
- ما فيش تاريخ (history) للتغييرات، ولا طريقة تعمل "rollback" لو تعديل معين طلع غلط.

**التوصية:** استخدم Supabase CLI (`supabase migration new ...`) وابدأ تسجّل كل تعديل SQL جديد كملف مرقّم جوه `supabase/migrations/`. حتى لو مش هتشغّل المشروع محليًا بقاعدة بيانات منفصلة، على الأقل هتبقى عندك نسخة كاملة من السكيمة في الجيت، وتقدر تعمل `supabase db diff` تقارن بيها.

### 2.2 مفيش أي اختبارات (Tests) خالص

طول الجلسات اللي فاتت، كل تأكيد إن حاجة شغالة كان بيتم يدويًا: أنا أعمل `npx next build` + `eslint`، وانت تجرب في المتصفح وتبعتلي سكرين شوت. الطريقة دي شغالة، بس معناها:
- أي تعديل جديد ممكن يكسر حاجة قديمة من غير ما حد يلاحظ إلا لما يحصل في اللعب الفعلي (وده حصل فعلاً أكتر من مرة في الجلسة دي — مثلاً باج "team.joined" اللي كان بيمنع اختيار الأسئلة، وباج التايمر اللي بيبدأ من 01:01 بدل 01:00).
- منطق اللعبة المهم (حساب النقاط، شروط الفوز، خصم نقاط الطقة) موجود جوه دوال SQL معقدة (`execute_strike`, `finalize_room_if_complete`, `resolve_room_question`) وده بالظبط النوع اللي لازم يتغطى باختبارات، لأن أي خطأ فيه بيأثر على نتيجة اللعبة نفسها.

**التوصية (مش لازم تعمل كل حاجة مرة واحدة):**
- ابدأ بأبسط حاجة: اختبارات لمنطق حساب النقاط/التكلفة اللي في `app/battle/page.jsx` (`unitSpecs`, حساب التكلفة) لأنه منطق JS بسيط تقدر تختبره بـ Vitest من غير ما تحتاج قاعدة بيانات.
- لو حبيت تتقدم أكتر، فيه أداة اسمها `pgTAP` بتخليك تكتب اختبارات لدوال الـ Postgres نفسها.

### 2.3 جدول تكلفة الوحدات (unitSpecs) متكرر في أكتر من مكان

تكلفة كل وحدة (جندي = 20، مدرعة = 100، دبابة = 200، طائرة = 400، غواصة = 500) موجودة حاليًا في **3 أماكن منفصلة**:
1. `app/battle/page.jsx` (كلاينت) — `unitSpecs`.
2. دالة `update_team_deployment` (SQL) — بتتأكد من نفس القيم وقت التوزيع.
3. دالة `execute_strike` (SQL) — بتستخدم نفس القيم لحساب الخصم وقت الإصابة.

الجلسة دي شهدت أكتر من باج سببه اختلاف بسيط بين النسختين دول (مرة اللغم كان تكلفته مختلفة بين الكلاينت والسيرفر، ومرة وحدة "مدرعة" الجديدة كانت ناقصة من نسخة السيرفر). المشكلة إنه لو حد غيّر رقم في مكان وناسي المكانين التانيين، اللعبة هتتكسر بصمت من غير أي error واضح.

**التوصية:** لو ممكن، خلي جدول التكلفة ده مصدر واحد بس — أبسط حل عملي: جدول صغير في قاعدة البيانات اسمه مثلاً `unit_specs` (unit_type, cost, limit)، والكلاينت يجيبه وقت التحميل بدل ما يكون hardcoded، والدوال في SQL تقرأ منه بدل الـ `CASE WHEN` المكرر. ده هيمنع تكرار الغلط ده تاني.

### 2.4 أدوات ("فزعات") معرّفة بس مش مستخدمة فعليًا

`lib/game-data.js` فيه تعريفات لأدوات زي `lifeline_call` (اتصال بصديق) و`double_chance` (فرصتين) و`the_hole` (الحفرة) و`the_detector` (الكاشف)، لكن الفرق حاليًا بتاخد بس 3 أدوات ثابتة (`radar_scan`, `shield`, `extra_strike`) عن طريق `create_game_room`. الأدوات التانية دي كود ميت (dead code) مش هيتفعّل أبدًا في اللعب الحالي، وده ممكن يلخبط أي حد يفتح الملف يفتكر إنها شغالة.

**التوصية:** لو مفيش نية تفعيلها قريب، احذفها من `TACTICAL_TOOL_DETAILS` عشان الملف يبقى معبّر عن الواقع الفعلي للعبة.

### 2.5 دالة `claim_team_slot` بقت orphaned تمامًا

من ساعة ما اتحول نظام دخول الفرق للـ token بدل الحساب (Phase A)، دالة `claim_team_slot` وعمود `teams.member_id` بقوا مش مستخدمين خالص في أي مكان في الكود الحالي. اتقرر وقتها الإبقاء عليهم "تفاديًا لأي كسر"، بس دلوقتي بعد ما البنية استقرت، الأفضل حذفهم عشان محدش يفتكر إنهم لسه جزء من التدفق الفعلي.

### 2.6 مكوّنات متكررة/متشابهة في `RefereeGameScreen.jsx`

الملف فيه أكتر من مكوّن بيعمل نفس الحاجة تقريبًا بستايلات مختلفة شوية: `TeamPillBar`, `TeamToolsCard`, `TeamStrikeStepper`, `HelperToolsSection`. ده نتيجة طبيعية لتعديلات كتير متتالية (منك ومني) على نفس الشاشة، بس النتيجة إن فيه تكرار في المنطق (كل واحد فيهم بيعرض اسم الفريق + أزرار الأدوات بطريقته). مش لازم تتصلح دلوقتي لأنك بتطوّر الاستايل بنفسك حاليًا، بس هي نقطة تستاهل "تنظيف" لما تستقر على الشكل النهائي.

### 2.7 أخطاء React موجودة من زمان ومتأجّلة

كل مرة عملنا `eslint` كان بيطلع نفس 5 أخطاء في `app/battle/page.jsx` (قاعدة `react-hooks/refs`) بتاعت قراءة `pendingBoardRef.current` مباشرة وقت الـ render بدل جوه event handler. اتأجلت كل مرة لأنها "موجودة من قبل" ومش هي سبب الباج اللي كنا بنصلحه وقتها، بس هي فعليًا مخالفة لقاعدة React رسمية ومكتوب في وصفها: ممكن تعمل حالة إن الشاشة ما تتحدّثش صح في بعض الحالات. تستاهل تتصلح في جلسة منفصلة مخصصة لها.

---

## 3. الثغرات الأمنية (اللي اتكشفت واتصلحت + الوضع الحالي)

الحاجات دي معظمها **اتصلحت فعلاً** خلال الجلسات اللي فاتت، بس بسجلها هنا عشان تبقى موثّقة في مكان واحد:

1. **دوال الأدمن كانت من غير أي تحقق صلاحية على مستوى السيرفر** (كان أي حد يقدر يستدعيها لو عرف اسمها) — اتصلحت بإضافة `is_admin()` check جوه كل دالة.
2. **تسريب خريطة الفريق (`teams.board`)** — كان أي مستخدم مسجّل يقدر يشوف توزيع جيش أي فريق في أي غرفة، حتى لو مش لاعب فيها، بسبب policy عام `USING(true)` + بث الـ Realtime لكل الأعمدة. اتصلح بنقل `board` لجدول منفصل `team_boards` بصلاحيات محدودة.
3. **`execute_strike`, `resolve_room_question`, `grant_extra_strikes` كانوا من غير أي تحقق هوية خالص** — أي مستخدم مسجّل دخول (مش بس الحكم) كان يقدر يستدعيهم على أي غرفة يعرف رقمها ويغيّر نتيجة اللعبة. اتصلح بإضافة تحقق `room.judge_id = auth.uid()`.
4. **حسابات لوحة التحكم كانت مربوطة بنفس نظام إيميلات الموقع** — يعني نفس الإيميل ميقدرش يتسجل مرتين (مرة كحساب لعب، مرة كأدمن). اتصلح بفصل كامل: إيميل داخلي مصطنع لأي حساب أدمن جديد + `login_email` منفصل تمامًا عن جدول `profiles`.

**الوضع الحالي (تصميم متعمّد، مش ثغرة):** جداول `game_rooms`, `teams`, `room_questions`, `combat_events` عندها policy قراءة عامة (`USING(true)`) لأي مستخدم مسجّل دخول (بما فيهم الفرق اللي بتدخل بالـ token من غير حساب). ده تصميم مقصود يناسب فكرة "أي حد معاه اللينك يقدر يلعب"، بس المهم توثيقه صراحة عشان محدش "يصلحه" بالغلط يفتكره باج، أو العكس يفتكره private وهو مش كده.

---

## 4. هل نستخدم Redux Toolkit؟

**الخلاصة: لأ، مش دلوقتي — ومش لازم تتعجل عليه.**

### ليه لأ؟

المشكلة اللي إنت حاسس بيها (كتر الـ `useState` في `app/battle/page.jsx`، وكتر الـ props اللي بتتبعت لـ `RefereeGameScreen`) هي فعلاً مشكلة حقيقية، بس Redux مش بالضرورة الحل الصح ليها، للأسباب دي:

1. **Redux معمول لحالة مشتركة بين أجزاء كتير من التطبيق مالهاش علاقة ببعض.** المشروع ده صفحات قليلة، كل صفحة شبه مستقلة عن التانية (`/battle`, `/admin`, `/my-games`). المشكلة عندك مش "حالة متبعثرة في كل التطبيق"، المشكلة إن **مكوّن واحد** (`battle/page.jsx`) كبر أوي وبقى فيه حتة كل حاجة. Redux هيحل مشكلة تانية غير اللي عندك.
2. **بيانات الـ Realtime (Supabase subscriptions) مش بتتظبط تلقائي مع Redux.** لسه هتحتاج تكتب نفس كود الـ `useEffect` + `setState` اللي عندك دلوقتي، وبعدين "تحقن" النتيجة جوه الـ store. يعني مش هيقلل الكود، هيزوّده شوية في البداية.
3. **تكلفة تعلّم وصيانة إضافية** على مشروع لسه بيتبني بسرعة، من غير أي خبرة Redux ظاهرة في الكود الحالي — وده وقت أفضل تصرفه في حاجات أهم زي الـ migrations والاختبارات (فقرة 2.1 و2.2 فوق) اللي فعلاً بتأثر على استقرار المشروع.

### إيه البديل الأنسب دلوقتي؟

بدل Redux، الحل الأخف واللي هيحل نفس المشكلة الفعلية:

- **قسّم `battle/page.jsx` لـ custom hooks منفصلة**: مثلاً `useRoomRealtime()` (بيرجع room/teams/questions/events ومسؤول عن الاشتراك في Realtime)، و`useDeployment()` (منطق توزيع الجنود)، و`useCombatActions()` (كل الـ RPC wrappers بتاعة الضرب والحل). كل hook بيرجع بس الحاجة اللي بتخصه، والصفحة نفسها تبقى أقصر وأوضح.
- لو حبيت تقلل الـ props اللي بتتبعت لـ `RefereeGameScreen`، تقدر تستخدم **React Context** بسيط (مش Redux) يلف الشاشة دي بس، ويوفّر البيانات المشتركة (room, teams, isBusy) من غير ما تتبعت prop prop لكل مكوّن فرعي.

**متى فعلاً تفكر في Redux Toolkit (أو حاجة زيه زي Zustand)؟** لو المشروع كبر لدرجة إن نفس البيانات (مثلاً بيانات المستخدم أو الغرفة) بقت محتاجة تتشارك بين صفحات كتير مش مرتبطة ببعض في المسار (routing tree)، أو لو عايز حاجات زي "تراجع/إعادة" (undo/redo) أو تتبع دقيق لكل تغيير حالة. المشروع ده لسه بعيد عن النقطة دي.

لو قررت مستقبلاً تحتاج حاجة من نوع Redux، فيه بديل أخف بكتير ومناسب لمشروع بالحجم ده اسمه **Zustand** — بيدّيك نفس فكرة الـ store المركزي، بس من غير الـ boilerplate الكتير بتاع Redux (actions, reducers, slices منفصلة). ده أقرب لحجم مشروعك من Redux Toolkit نفسه.

---

## 5. توصيات مرتّبة حسب الأولوية

1. **(عالي) ابدأ تسجّل كل تعديل SQL كملف migration في الريبو** — أقل مجهود، أعلى فايدة، وبيحميك لو حصل أي مشكلة في قاعدة البيانات الحية.
2. **(عالي) وحّد جدول تكلفة الوحدات في مكان واحد** — عشان تمنع تكرار نفس فئة الباجات اللي حصلت أكتر من مرة.
3. **(متوسط) نظّف الكود الميت**: `claim_team_slot`, الأدوات الغير مفعّلة (`lifeline_call` وأخواتها), الأعمدة اللي بقت مش مستخدمة زي `teams.member_id`.
4. **(متوسط) قسّم `battle/page.jsx` لـ custom hooks** بدل إضافة Redux — هتحس بفرق حقيقي في سهولة القراءة والتعديل من غير أي مكتبة جديدة.
5. **(منخفض حاليًا، لكن مهم مستقبلاً) ابدأ اختبارات بسيطة** لمنطق حساب النقاط والتكلفة أول حاجة، لأنه أسهل جزء تختبره وأخطر جزء لو غلط.
6. **متتعجلش على Redux Toolkit** إلا لو المشروع كبر فعلاً بشكل تاني (صفحات وحالة مشتركة كتير) — دلوقتي مش هيحل المشكلة اللي حاسس بيها.

---

*التقرير ده بناءً على حالة المشروع لحد تاريخ كتابته — أي تعديلات جديدة بعد كده ممكن تغيّر بعض النقاط.*
